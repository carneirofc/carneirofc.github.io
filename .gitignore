# dependencies
node_modules/

# next.js — unanchored so a stray `next dev` rooted in a subdirectory
# (e.g. src/app/.next/) is ignored too.
.next/
/out/

# velite
/.velite/
/public/static/

# misc
.DS_Store
*.pem
npm-debug.log*
.env
.env.*

# typescript
*.tsbuildinfo

# agents
CLAUDE.md
